{
    "name": "labs-cucumber-js",
    "version": "0.0.1",
    "private": true,
    "dependencies": {
        "cucumber": "*",
        "zombie": "*"
    },

    "scripts": {
    	"test": "cucumber.js --format pretty tests"
    }
}
